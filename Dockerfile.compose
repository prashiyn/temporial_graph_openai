FROM python:3.12-slim

ENV PYTHONDONTWRITEBYTECODE=1 \
    PYTHONUNBUFFERED=1

WORKDIR /app

RUN pip install --no-cache-dir uv

COPY . .

RUN uv sync

EXPOSE 8080

CMD ["uv", "run", "uvicorn", "temporal_graph.api.main:app", "--host", "0.0.0.0", "--port", "8080"]
